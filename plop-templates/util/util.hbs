/**
 * @packageDocumentation
 * @module @tomato-js/{{scope}}
 */
/**
 * 函数描述
 *
 * 新增于v{{version}}
 *
 * 脚本举例
 * ```
 *   import { {{name}} } from '@tomato-js/{{scope}}'
 * ```
 */
export function {{name}}() {
  
}
